name: First Workflow
on: workflow_dispatch
jobs: 
  first-job: 
    runs-on: ubuntu-latest
    steps:
      - name: Print greeting
        run: | 
          echo "Hello world"
          echo "Run second shell command"
      - name: Print goodby
        run: echo "Done -  Goodbye!"
